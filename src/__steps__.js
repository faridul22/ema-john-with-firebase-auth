/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm install firebase
 * 4. save firebase config and export default app
 * 5. Build> Authentication> get started> enable sign in method
 * 6. create sign up and login route
*/


/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider in main.jsx
*/


/**
 * HOSTING
 * -----------
 * One time per PC
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * 
 * For each project one time
 * -------------------------
 * 1.firebase init
 * 2.proceed=> Hosting: Configure files for Firebase Hosting and (optionally) set up GitHub Action deploys
 * 3.Project Setup=> Use an existing project 
 * 4.Select a Firebase project for this directory
 * 5.Hosting Setup
 * 6.What do you want to use as your public directory? dist
 * 7.Configure as a single-page app (rewrite all urls to /index.html)? Yes
 * 8.Set up automatic builds and deploys with GitHub? No
 * 
 * 
 * For every time deploy
 * 1.npm run build
 * 2.firebase deploy 
*/